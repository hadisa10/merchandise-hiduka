import { MainResetPasswordView } from 'src/sections/auth/main';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Reset Password',
};

export default function ResetPasswordPage() {
  return <MainResetPasswordView />;
}
